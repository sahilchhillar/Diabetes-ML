name: diabetes-prediction-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy>=1.23
  - pandas>=1.5
  - scikit-learn>=1.2
  - flask>=2.2
  - pip:
      - azureml-defaults
      - inference-schema
